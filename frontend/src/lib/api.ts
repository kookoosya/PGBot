/** Backward-compatible re-export — импорты `@/lib/api` остаются без изменений. */
export { api, API_BASE } from "./api/index";
export type { ApiClient } from "./api/index";
export * from "./api/types";
